package hackclubchess;

public class Board {
    // We will use flattaned 2D array. There are 64 squares, so array length 64 
    // To convert between position and rank&file numbers we do:
    // *    pos/8 -> rank
    // *    pos%8 -> file
    int[] board = new int[64];
    
    // Fill out rest of class
}